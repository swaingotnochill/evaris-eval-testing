[project]
name = "evaris-eval-testing"
version = "0.1.0"
requires-python = ">=3.12"
dependencies = [
    "inspect-ai>=0.3,<0.4",
    "langchain>=1.0.0",
    "langchain-openai>=1.0.0",
    "openai>=1.0.0",
]
